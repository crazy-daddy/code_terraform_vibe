# Seed Maker automation: fair brute-force sweep of untried life-form triples
# until every seed species is discovered (stage A). See lib/seed_maker.py.

from seed_maker import SeedMakerController

controller = SeedMakerController(self)
controller.run()
